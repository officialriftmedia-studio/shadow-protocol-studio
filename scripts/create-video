#!/usr/bin/env bash
# create-video — entrypoint for Shadow Protocol Studio
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${SCRIPT_DIR}:${PYTHONPATH:-}"

python3 -m shadow_protocol.cli "$@"
